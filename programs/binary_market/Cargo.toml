[package]
name = "binary_market"
version = "0.1.0"
description = "Binary prediction market (UP/DOWN rounds) with Pyth oracle"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "binary_market"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = { version = "0.32.1", features = ["init-if-needed"] }
