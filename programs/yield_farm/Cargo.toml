[package]
name = "yield_farm"
version = "0.1.0"
description = "$PROFIT yield farming — stake tokens, earn tiered rewards, back binary market liquidity"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "yield_farm"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = { version = "0.32.1", features = ["init-if-needed"] }
